package com.valentech.tags.tageditor.model;

/**
 * Interface that the model uses to interact with the view of the tag editor system
 */
public interface ITagEditorM2VAdapter {

    ITagEditorM2VAdapter NULL_ADAPTER = new ITagEditorM2VAdapter() {};
}
